# WALLABY hi-res manifest format

The canonical contract, Core record variant, production preflight rules, and
no-download semantics are maintained in
[`docs/manifest-contract.md`](docs/manifest-contract.md).

This compatibility page remains so existing links do not break.
