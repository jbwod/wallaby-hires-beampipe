# Historical commands (non-operational)

The original contents described an obsolete Setonix experiment and embedded
personal usernames, email addresses, allocation names, job identifiers, scratch
paths, and an unpinned ASKAPsoft image path. They have been retired to avoid
copy/paste deployment and disclosure risk.

Do not use this file as an operations guide. Use `docs/operator-runbook.md`, the
current `*-beampipe.graph` files, and deployment-managed secret/profile values.
